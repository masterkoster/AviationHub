# Marketing screenshots for the landing page

Drop these PNGs here and the landing page (`app/components/v1-landing-page.tsx`,
`BrowserScreenshot` frames) picks them up automatically. Until a file exists, its
frame shows a clean branded-gradient placeholder (no "coming soon" text).

Recommended: ~16:9, 1600×900 (or 1280×720), captured in the app's dark theme for
consistency. Crop to the content area (the browser frame is drawn by the page).

| File                    | Capture from            | Public? |
|-------------------------|-------------------------|---------|
| `planner.png`           | `/fuel-saver` (planner) | ✅ yes  |
| `fuel-map.png`          | `/fuel-saver` (fuel map)| ✅ yes  |
| `logbook.png`           | `/logbook`              | login   |
| `clubs.png`             | `/flying-club`          | login   |
| `weather.png`           | `/fuel-saver` (Weather tab) | ✅ yes |
| `weight-balance.png`    | `/fuel-saver` (W&B tab) | ✅ yes  |

The two flagship shots (`planner`, `fuel-map`) need no login — `/fuel-saver` is public.
